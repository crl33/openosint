# OpenOSINT Cloud — Heroku requirements
#
# Installs the openosint engine from the local source tree (pulls all deps
# declared in pyproject.toml), then adds the cloud-specific packages.
#
# Heroku's Python buildpack runs:  pip install -r requirements.txt
-e .

# Cloud gateway
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
asyncpg>=0.29.0
httpx>=0.27.0
pydantic>=2.0.0
